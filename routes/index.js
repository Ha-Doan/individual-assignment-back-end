
  const classes = require('./classes')
  const users = require('./users')
  const sessions = require('./sessions')
  const students = require('./classes/students')
  const testclasses = require('./testclasses')
  const teststudents = require('./testclasses/teststudents')
  module.exports = {
    classes,
    users,
    sessions,
    students,
    testclasses,
    teststudents,
  }
